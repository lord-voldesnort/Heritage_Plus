# Loaded automatically by Vite/Vitest when running in "test" mode.
# apiClient.ts defaults to a relative "/api" base (correct for the browser +
# dev/prod reverse proxy), but a relative URL can't resolve under Node's
# fetch with no page origin — so tests need an absolute URL to the live API.
VITE_API_URL=http://localhost:3000/api
